

const Player = ({player}) => {
    const {name} = player;
    console.log(player)
    return (
        <div>
            <div>
                
            </div>
        </div>
    );
};

export default Player;